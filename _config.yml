sass:
  style: compressed

defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

  - scope:
      path: "assets/css"
    values:
      layout: null

# Inclui o CSS personalizado
include:
  - assets/css/style.css
